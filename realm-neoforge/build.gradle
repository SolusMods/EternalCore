loom {
    accessWidenerPath = project(":realm-common").loom.accessWidenerPath
}

dependencies {
    implementation project(path: ":network-common", configuration: 'transformProductionNeoForge')
    implementation project(path: ":storage-common", configuration: 'transformProductionNeoForge')
}